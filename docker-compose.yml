version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/code
      - /code/node_modules
      - ${HOME}/.ssh:/home/node/.ssh:ro
    ports:
      - 3000:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
